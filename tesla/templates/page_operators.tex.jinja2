\begin{tabular}{ p{0.3\textwidth} | p{0.6\textwidth} }
    \textbf{Page} & \textbf{Operators with counts}\\
    {% for page in page_names %}\hline
    {{ page }} & {% for operator in pages[page].keys() %}{{ operator | tex_escape }}: {{ pages[page][operator] | tex_escape }}, {% endfor %}\\
    {% endfor %}
\end{tabular}
