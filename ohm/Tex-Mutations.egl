\begin{tabular}{ c|c|c }
    Mutation Operator & Description & Guideline \\
    [% for (mutator in mutatorSet.mutators) { %]
        \hline
        [%= mutator.name %] & [%= mutator.shortDesc %] & [% if (mutator.guidelines.size() > 0) { %][%= mutator.guidelines[0].name %][% } else { %]-[% } %] \\
    [% } %]
\end{tabular}
